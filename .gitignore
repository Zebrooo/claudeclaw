node_modules/
dist/
agent/runner/dist/
store/
data/
groups/
# Pipeline agent templates are committed (override above)
!groups/pipeline_*/
!groups/pipeline_*/CLAUDE.md
!groups/pipeline_*/scan-server.sh
!groups/pipeline_*/skills/
logs/
.env
# never commit .env backups — they contain real tokens
.env.bak*
.env.*.bak
*.pid

# Installable extensions (per-instance, not checked in)
extensions/

# Brainstorm sessions
.superpowers/
.worktrees/
