# Web Search Skill

## Web Search
Use the built-in `WebSearch` tool to search the internet.

## Read a web page
Use the built-in `WebFetch` tool to fetch and read a URL.

## GitLab
```bash
# List MRs
glab mr list --repo "$GITLAB_REPO"

# Create issue
glab issue create --title "$TITLE" --description "$DESC" --repo "$GITLAB_REPO"
```
